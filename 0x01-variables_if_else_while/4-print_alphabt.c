#include <stdio.h>
/**
 * main - Prints alphabets A to Z in lower case without e and q
 *
 * Return: 0
 */
int main(void)
{
	char letter = a;

	for (letter = 'a'; letter <= 'z'; letter++)
		while (a !='e' && a != 'q')
		{
		putchar(letter);
		}

	putchar('\n');
	return (0);
}
