#include <stdio.h>
/**
 * main - Prints alphabets A to Z in lower case without e and q
 *
 * Return: 0
 */
int main(void)
{
	char letter

	for (letter = 'a'; letter <= 'z'; letter++)
		{
		putchar(letter);
		}
	do while (a !='e' && a != 'q')

	putchar('\n');
	return (0);
}
